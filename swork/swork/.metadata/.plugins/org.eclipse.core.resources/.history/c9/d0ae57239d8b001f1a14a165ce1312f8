<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardDAOMapper">

	<select id="getBoard" resultType="board">
<!-- 	<select id="getBoard" resultType="com.springbook.biz.board.BoardVO"> -->
		<!-- select 쿼리문 기술 및 바인드변수 기술 -->
		<!-- 반드시 바인드변수는 보내는 파라미터에 있는 이름과 동일하게 기술 -->
		SELECT * FROM BOARD WHERE SEQ = #{seq}
	</select>

	<insert id="insertBoard">
		INSERT INTO BOARD(TITLE, WRITER, CONTENT, FILENAME) VALUES(#{title},
		#{writer}, #{content}, #{filename})
	</insert>
	
	<update id="">
		UPDATE BOARD SET TITLE = #{title}, WRITER = #{writer}, CONTENT = #{content}, WHERE SEQ = #{seq}
	</update>
	
	<delete id="">
		DELETE FROM BOARD WHERE SEQ = #{seq}
	</delete>
	
	<select id="">
		SELECT * FROM BOARD WHERE SEQ = #{seq}
	</select>
</mapper>