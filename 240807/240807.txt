#JavaScript
    : 함수형 언어
    
    함수형 언어의 특징
    - 자료형에 국한되지 않는다. => 초기값을 1로 할당했더라도 재할당 시 "Hello" 가능하다. 

#자바스크립트의 전역/지역
    * 전역 : var 키워드를 포함하고 있는 페이지 내에서만
    * 지역 : let 키워드가 선언되어있는 범위 내에서만

#자바스크립트의 값 할당
    * undefined : 선언만 한 경우 자동으로 할당됨.
    * 

#호이스팅 
    : 변수의 선언보다 값 할당이 먼저 작성된 경우, 강제로 선언부를 값 할당보다 상단으로 끌어올려준다.

    * 호이스팅이 가능한 조건
        1. 변수를 var로 선언한 경우
        2. 선언적함수로 선언하는 경우

#자바스크립트의 자료형
    {} => object
    [] = Array